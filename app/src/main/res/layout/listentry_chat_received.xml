<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="0sp"
                android:layout_marginStart="0sp"
                android:layout_marginRight="50sp"
                android:layout_marginEnd="50sp"
                android:background="@drawable/chat_background">

    <ImageView
        android:id="@+id/chat_received_avatar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/user"
        android:contentDescription="@string/content_desc_avatar"/>

    <TextView
        android:id="@+id/chat_received_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5sp"
        android:layout_marginLeft="5sp"
        android:layout_toEndOf="@id/chat_received_avatar"
        android:layout_toRightOf="@id/chat_received_avatar"
        android:textSize="16sp"
        android:textColor="#777777"/>

    <TextView
        android:id="@+id/chat_received_output"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="5sp"
        android:layout_marginLeft="5sp"
        android:layout_toEndOf="@id/chat_received_avatar"
        android:layout_toRightOf="@id/chat_received_avatar"
        android:layout_below="@id/chat_received_timestamp"
        android:textSize="20sp"/>

</RelativeLayout>